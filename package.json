{
  "private": true,
  "scripts": {
    "test": "eslint . --ext .json --ext .cjs",
    "lint:fix": "eslint . --fix --ext .json --ext .cjs"
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-plugin-json": "^1.4.0",
    "eslint-config-wikimedia": "^0.12.0"
  }
}
